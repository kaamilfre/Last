git init     ||   folder ka ayu ka dhiga git Project

git status   ||   xaalada files ka add la dhahay iyo kuwa hadhay

git add filename ||  file ka lagu daraayo

git add .     || ku dar dhaman files ka

git commit -m "message" || wuxu kaydiya Version ka

git commit --amend --reset-author || wuxu update author ama messege ka

git remote add origin REPO_URL || project ga wuxu ku xidha gitHub

git remote -v  || wuxuu muujiya GITHUB lInks ka

git remote remove origin || wuxu jara connection ka GitHub 

git branch -M main || magaca Branch bedel

git push -u origin main  ||  gitHub ayuu u dira files ka 

git branch test || version cusub ayuu sameeya

git push -u origin test || branch-ka GitHub ayuu diraa.

git reset filename ||  ile-ka add waa laga noqdaa.

git rm --cached filename ||  tracking ayuu joojiyaa.

git rm -f filename || file + Git tracking waa la tirtiraa.

rm -rf .git  ||  Git history oo dhan ayuu nadiifiyaa.

( CMD )  rmdir /s /q .git     || lamid 

( CMD ) rd /s /q .git         || Lamid 

git config --global user.name "YourName" || magaca

git config --global user.email "YOUR_EMAIL" || Email ka

git config --global core.autocrlf true  ||  Waxa uu qabto: LF/CRLF warning xaliyaa

echo node_modules/ > .gitignore  ||  files qaarkood Git ma gelayo.

git rm filename || ka saar file computer + GitHUb 

( kadibna )  git commit -m "remove README"
git push

git rm --cached filename || GitHub kaliya ka saar


( kadib) git commit -m "stop tracking file"
git push


git rm -r --cached node_modules || folder ka dhan ka saaar GitHub

( kadibna ) sidi hore si la mida 
 

                    ( SOO KOOBID )


     git init
git add .
git commit -m "message"
git push
git status  




      ( MARKASTA LA ADEGSADO MARKA HOWLAHA LA DHAMAYS TIRO  )



      git add .
git commit -m "update"
git push



